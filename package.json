{
  "name": "unifi-protect-detections",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "health": "node -e \"fetch('http://localhost:' + (process.env.PORT || 8899) + '/api/health').then(async r => { if (!r.ok) throw new Error('HTTP ' + r.status); console.log(await r.text()); }).catch(e => { console.error(e.message); process.exit(1); })\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "unifi-protect": "^4.28.0"
  }
}